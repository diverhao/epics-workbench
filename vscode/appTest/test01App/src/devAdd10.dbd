# `DevAdd10` is defined in `devAdd10.c`
# `Add10` is the device name used in DTYP in record definition
device(ai, INST_IO, aaabbbccc, "aaabbbccc")

driver(AABBCC)

variable(myParameter, double)


function(myFunction)