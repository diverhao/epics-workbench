{
  "$schema": "../schemas/tmlanguage.json",
  "name": "EPICS PV List",
  "scopeName": "source.epics.pvlist",
  "patterns": [
    {
      "include": "#comments"
    },
    {
      "include": "#macro-definitions"
    },
    {
      "include": "#record-lines"
    }
  ],
  "repository": {
    "comments": {
      "patterns": [
        {
          "name": "comment.line.number-sign.epics.pvlist",
          "begin": "^\\s*#",
          "end": "$"
        }
      ]
    },
    "macro-definitions": {
      "patterns": [
        {
          "name": "meta.definition.macro.epics.pvlist",
          "begin": "^\\s*([A-Za-z_][A-Za-z0-9_]*)(\\s*)(=)(\\s*)",
          "beginCaptures": {
            "1": {
              "name": "variable.other.assignment.epics.pvlist"
            },
            "3": {
              "name": "keyword.operator.assignment.epics.pvlist"
            }
          },
          "end": "$",
          "patterns": [
            {
              "include": "#macro-expressions"
            },
            {
              "name": "string.unquoted.macro-value.epics.pvlist",
              "match": "\\S+"
            }
          ]
        }
      ]
    },
    "record-lines": {
      "patterns": [
        {
          "name": "meta.record.epics.pvlist",
          "begin": "^\\s*(?!#)(?![A-Za-z_][A-Za-z0-9_]*\\s*=)(\\S+)",
          "beginCaptures": {
            "1": {
              "name": "entity.name.record.epics.pvlist"
            }
          },
          "end": "$",
          "patterns": [
            {
              "include": "#macro-expressions"
            }
          ]
        }
      ]
    },
    "macro-expressions": {
      "patterns": [
        {
          "name": "variable.other.macro.epics.pvlist",
          "match": "\\$\\((?:[^)=\\s]+)(?:=[^)]*)?\\)|\\$\\{[^}\\s]+\\}"
        }
      ]
    }
  }
}
